# 🚀 Пошаговая инструкция: Деплой на Render.com

## ✅ Все файлы готовы!

Я уже создал все необходимые файлы:
- ✅ `requirements.txt` - зависимости Python
- ✅ `runtime.txt` - версия Python
- ✅ `.gitignore` - игнорируемые файлы
- ✅ `README.md` - описание проекта
- ✅ `web.py` - обновлен для использования переменных окружения

---

## 📋 Шаг 1: Установите Git (если еще не установлен)

### Скачайте Git:
https://git-scm.com/download/win

### После установки перезапустите PowerShell

---

## 📋 Шаг 2: Создайте GitHub репозиторий

1. Зайдите на https://github.com
2. Нажмите зеленую кнопку **"New"** (или "+" → "New repository")
3. Назовите репозиторий: `discord-bot-dashboard`
4. Оставьте **Public** (или выберите Private, если хотите)
5. **НЕ** ставьте галочки на "Add README" и ".gitignore" (у нас уже есть)
6. Нажмите **"Create repository"**

---

## 📋 Шаг 3: Загрузите код на GitHub

Откройте PowerShell в папке проекта и выполните:

```powershell
# Перейдите в папку проекта
cd C:\Users\kompd\.gemini\antigravity\scratch\discord_bot

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Bot Dashboard Pro"

# Добавьте remote
git remote add origin https://github.com/ВАШ-ЛОГИН/discord-bot-dashboard.git

# Переименуйте ветку в main
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

**Важно!** Замените `ВАШ-ЛОГИН` на ваш настоящий GitHub логин!

---

## 📋 Шаг 4: Разверните на Render.com

### 4.1. Зарегистрируйтесь на Render

1. Зайдите на https://render.com
2. Нажмите **"Get Started"**
3. Войдите через GitHub (это удобнее)

### 4.2. Создайте новый Web Service

1. В дашборде Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий:
   - Нажмите **"Connect account"** если нужно
   - Найдите `discord-bot-dashboard` в списке
   - Нажмите **"Connect"**

### 4.3. Настройте деплой

Заполните форму:

**Name:** `bot-dashboard` (или любое имя)

**Region:** Выберите ближайший регион (например, Frankfurt)

**Branch:** `main`

**Runtime:** `Python 3`

**Build Command:** 
```
pip install -r requirements.txt
```

**Start Command:**
```
python web.py
```

### 4.4. Установите переменные окружения

Прокрутите вниз до раздела **"Environment Variables"** и добавьте:

| Key | Value |
|-----|-------|
| `CLIENT_ID` | `1211664015646916670` |
| `CLIENT_SECRET` | `ykwvV-Jg6WaWey-bsejTTEPTsho2NiAd` |
| `SECRET_KEY` | `super-secret-key-change-me-123` (или сгенерируйте новый) |
| `REDIRECT_URI` | `https://ВАШ-ДОМЕН.onrender.com/callback` |

**ВАЖНО!** `REDIRECT_URI` вы узнаете после создания сервиса!

### 4.5. Создайте сервис

1. Нажмите **"Create Web Service"**
2. Render начнет деплой (займет 2-3 минуты)
3. Дождитесь статуса **"Live"** (зеленый)

---

## 📋 Шаг 5: Настройте Discord OAuth

### 5.1. Скопируйте URL вашего сайта

После деплоя вы получите URL вида: `https://bot-dashboard-xyz.onrender.com`

### 5.2. Обновите Discord Application

1. Зайдите на https://discord.com/developers/applications
2. Выберите ваше приложение
3. Перейдите в **OAuth2** → **General**
4. В разделе **Redirects** добавьте:
   ```
   https://bot-dashboard-xyz.onrender.com/callback
   ```
   (замените на ваш реальный домен)
5. Нажмите **"Save Changes"**

### 5.3. Обновите переменную окружения на Render

1. Вернитесь в Render → ваш сервис
2. Перейдите в **Environment**
3. Обновите `REDIRECT_URI` на:
   ```
   https://ваш-реальный-домен.onrender.com/callback
   ```
4. Нажмите **"Save Changes"**
5. Сервис автоматически перезапустится

---

## 🎉 Готово!

Ваш сайт теперь доступен по адресу: `https://ваш-домен.onrender.com`

### Что дальше?

✅ Сайт работает 24/7
✅ HTTPS автоматически настроен
✅ При каждом push в GitHub - автоматический деплой
✅ Бесплатно!

### ⚠️ Важные моменты:

**Бесплатный план Render:**
- Сайт "засыпает" после 15 минут неактивности
- Первый запрос после "сна" будет долгим (30-60 секунд)
- Для постоянной работы нужен платный план ($7/месяц)

**Как избежать "сна":**
- Используйте сервис мониторинга (например, UptimeRobot)
- Или перейдите на Railway.app (детали в DEPLOYMENT_GUIDE.md)

---

## 🆘 Если что-то не работает

### Логи на Render:
1. Откройте ваш сервис на Render
2. Вкладка **"Logs"**
3. Смотрите ошибки

### Распространенные проблемы:

**"Build failed":**
- Проверьте правильность `requirements.txt`
- Убедитесь, что все файлы загружены на GitHub

**"Application failed to start":**
- Проверьте логи
- Убедитесь, что `web.py` запускается локально

**Discord OAuth не работает:**
- Проверьте правильность `REDIRECT_URI` в обеих местах (Discord + Render)
- URL должен быть HTTPS, не HTTP

---

---

## 📋 Шаг 6: (Опционально) Подключите свой домен

Чтобы убрать `.onrender.com` из ссылки и сделать красивый адрес (например, `mysite.com`), вам нужно купить домен.

### 6.1. Купите домен
Купите домен у любого регистратора (Reg.ru, Namecheap, GoDaddy и т.д.).
- Стоимость `.ru` домена ~200-300 рублей/год.
- Стоимость `.com` домена ~1000-1500 рублей/год.

### 6.2. Добавьте домен в Render
1. Зайдите в ваш сервис на **Render Dashboard**.
2. Перейдите в вкладку **"Settings"**.
3. Прокрутите до раздела **"Custom Domains"**.
4. Нажмите **"Add Custom Domain"**.
5. Введите ваш домен (например, `mysite.com`).
6. Нажмите **"Save"**.

### 6.3. Настройте DNS (у регистратора домена)
Render покажет вам, какие записи нужно добавить в настройках DNS вашего домена.

Обычно нужно добавить две записи:
1. **CNAME** запись:
   - Name (Имя): `www`
   - Value (Значение): `ваше-приложение.onrender.com`
2. **A** запись (для корневого домена):
   - Name (Имя): `@`
   - Value (Значение): `216.24.57.1` (IP адрес Render, проверьте актуальный в дашборде!)

### 6.4. Проверка и HTTPS
1. Render автоматически проверит DNS записи (может занять до 24 часов, обычно 5-10 минут).
2. Как только DNS обновятся, Render автоматически выпустит SSL сертификат (HTTPS).

**Важно:** Не забудьте обновить `REDIRECT_URI` в Discord Developer Portal и переменную окружения в Render на ваш НОВЫЙ домен!

---

## 📞 Нужна помощь?

Если что-то не получается - покажите мне ошибку из логов Render, и я помогу! 🚀
